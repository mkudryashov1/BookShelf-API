REST API для управления личной библиотекой книг.  
Pet-проект для практики backend-разработки на Go.

Стек

- Go (net/http + chi)
- PostgreSQL
- database/sql + pgx
- Docker + Docker Compose

Запуск

*bash*
docker compose up --build